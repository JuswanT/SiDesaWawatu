services:
  # ============================================
  # OpenSID Wawatu
  # ============================================
  app-wawatu:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: opensid-wawatu
    restart: unless-stopped
    ports:
      - "6000:80"
    volumes:
      - ./desa_wawatu:/var/www/html/desa
      - ./storage_wawatu:/var/www/html/storage
      - ./backup_inkremental:/var/www/html/backup_inkremental
    environment:
      - CI_ENV=development
    depends_on:
      db:
        condition: service_healthy
    networks:
      - opensid-network

  # ============================================
  # OpenSID Tanjung Tiram
  # ============================================
  app-tanjungtiram:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: opensid-tanjungtiram
    restart: unless-stopped
    ports:
      - "6001:80"
    volumes:
      - ./desa_tanjungtiram:/var/www/html/desa
      - ./storage_tanjungtiram:/var/www/html/storage
      - ./backup_inkremental:/var/www/html/backup_inkremental
    environment:
      - CI_ENV=development
    depends_on:
      db:
        condition: service_healthy
    networks:
      - opensid-network

  # ============================================
  # OpenSID Lalowaru
  # ============================================
  app-lalowaru:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: opensid-lalowaru
    restart: unless-stopped
    ports:
      - "6002:80"
    volumes:
      - ./desa_lalowaru:/var/www/html/desa
      - ./storage_lalowaru:/var/www/html/storage
      - ./backup_inkremental:/var/www/html/backup_inkremental
    environment:
      - CI_ENV=development
    depends_on:
      db:
        condition: service_healthy
    networks:
      - opensid-network

  # ============================================
  # OpenSID Mata Wawatu
  # ============================================
  app-matawawatu:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: opensid-matawawatu
    restart: unless-stopped
    ports:
      - "6003:80"
    volumes:
      - ./desa_matawawatu:/var/www/html/desa
      - ./storage_matawawatu:/var/www/html/storage
      - ./backup_inkremental:/var/www/html/backup_inkremental
    environment:
      - CI_ENV=development
    depends_on:
      db:
        condition: service_healthy
    networks:
      - opensid-network

  # ============================================
  # MySQL Database
  # ============================================
  db:
    image: mysql:8.0
    container_name: opensid-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-rahasia123}
      MYSQL_DATABASE: ${DB_DATABASE:-opensid}
      MYSQL_USER: ${DB_USERNAME:-opensid}
      MYSQL_PASSWORD: ${DB_PASSWORD:-opensid123}
    ports:
      - "3309:3306"
    volumes:
      - opensid-db-data:/var/lib/mysql
    command: >
      --default-authentication-plugin=mysql_native_password
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_general_ci
      --sql-mode="STRICT_TRANS_TABLES,NO_ENGINE_SUBSTITUTION"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_ROOT_PASSWORD:-rahasia123}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - opensid-network

  # ============================================
  # phpMyAdmin (Opsional - untuk kelola database)
  # ============================================
  phpmyadmin:
    image: phpmyadmin:latest
    container_name: opensid-phpmyadmin
    restart: unless-stopped
    ports:
      - "8888:80"
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-rahasia123}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - opensid-network

volumes:
  opensid-db-data:
    driver: local

networks:
  opensid-network:
    driver: bridge
